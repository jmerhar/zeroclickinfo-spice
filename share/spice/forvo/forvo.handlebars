<div class="tile  tile--c--n  tile--forvo forvo-audio" type="audio/mp3" title="{{original}}" data-stream="/audio/?u={{standard_pronunciation.pathmp3}}" id="sound_{{id}}">
	    <div class="tile__media">
        <div class="audio-controls">
            <span class="audio-controls__action  ddgsi">►</span>
        </div>
    </div>
    <div class="tile__body">
        <a class="tile__title" href="{{link}}" title="{{original}}">{{ellipsis original 45}}</a>
        <div class="tile__description">
			<span class="sidenote">({{standard_pronunciation.langname}}, {{forvo_sex standard_pronunciation.sex}})
			</span>
        </div>
    </div>
</div>